#![allow(clippy::let_unit_value)]
#![allow(non_snake_case)]
use proconio::input;

fn main() {
    input! {
        N:usize,
        mut A:[u32;N+1],
        mut B:[u32;N]
    }
    B.append(0_u32);
    let mut ans = 0;
    let mut prev=0;
    for i in 0..N {
        let mons=A[] 
    }

    println!("{}", ans);
}
