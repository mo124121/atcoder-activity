{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build",
            "type": "cargo",
            "command": "build",
            "args": [
                "--bin=${fileBasenameNoExtension}",
                "--manifest-path=${workspaceFolder}/rust/Cargo.toml"
            ],
        }
    ]
}